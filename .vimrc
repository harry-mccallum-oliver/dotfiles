" Let Vim set the title of the terminal window
set title

" Show the line numbers
set number

" Show the cursor line and column numbers in the status line
set ruler

" Highlight the cursor line
set cursorline

" When starting a new line, copy the indent from the current line
set autoindent

" Use the default syntax highlighting colours
syntax on
